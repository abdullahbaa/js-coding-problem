function calculateFinalScore(obj) { 
    let number = (100,99,80);
typeof testScore === number ( testScore <= 50) ;
typeof schoolGrade === number ( schoolGrade <=30); 
typeof  isFFamily === boolean;

}
